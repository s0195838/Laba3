CREATE OR REPLACE TRIGGER cart_item_trigger
BEFORE INSERT ON CART_ITEM
FOR EACH ROW
BEGIN
    IF :NEW.id IS NULL THEN
        SELECT NVL(MAX(id), 0) + 1 INTO :NEW.id FROM CART_ITEM;
    END IF;
END;
/
